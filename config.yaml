# Reference only — these values are hardcoded in vetbot/ingest.py and retriever.py
# Edit the code directly if you need to change them

collection:    vet-disease
embedding:     text-embedding-3-large
routing_llm:   gpt-4o-mini
diagnosis_llm: gpt-4o
chunk_size:    600
chunk_overlap: 150
top_k:         5
batch_size:    20   # number of chunks uploaded per HTTP request
